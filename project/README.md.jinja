<!-- Seeded from the template channel (project kind) at
     birth; this file is the workspace's own. Edit it directly:
     the template never rewrites it.
-->
# {{ project_name }}

{{ project_description }}

## Development

`{{ runner_prog }} check` runs the whole local gate; CI runs the
same command. `{{ runner_prog }} sync` brings the checkout, the
delivered content, and the environment current. A fresh machine
enters with `source setup.sh`.
