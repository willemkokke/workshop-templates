<!-- Seeded from the template channel (project kind) at
     birth; this file is the workspace's own. Edit it directly:
     the template never rewrites it.
-->
# {{ project_name }}

{{ project_description }}

The rendered site lists each package's pages, API reference, and
changelog in the navigation, with the workspace's releases derived
from its receipt tags.
