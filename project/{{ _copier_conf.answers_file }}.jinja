# Managed by copier: this instance's identity and template
# provenance. `{{ runner_prog }} new.package` appends to `packages`; edit other
# values only when the workspace itself changes.
{{ _copier_answers|to_nice_yaml -}}
