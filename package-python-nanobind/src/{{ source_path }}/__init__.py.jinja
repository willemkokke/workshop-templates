# Seeded from the template channel (package-python-nanobind kind) at
# birth; this file is the workspace's own. Edit it directly:
# the template never rewrites it.
"""{{ package_description }}"""

from __future__ import annotations

from {{ import_path }}._native import native_hello

__all__ = ["__version__", "hello", "native_hello"]

__version__ = "0.0.0"


def hello() -> str:
    """Return the placeholder greeting; replace with the real API."""
    return "hello from {{ package_name }}"
