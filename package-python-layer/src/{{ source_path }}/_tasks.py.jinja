"""{{ import_path }}'s task surface, mounted through the layers list.

The workspace contract's ``[workspace] layers`` entry is the whole
of discovery, and the branded App mounts the same module. The group
below is this layer's namespace: hang the layer's own verbs on it,
or leave it empty while the layer ships content alone.
"""

from footman import group

{{ project_slug }} = group(
    "{{ project_slug }}", help="the {{ project_slug }} layer's verbs"
)

# @{{ project_slug }}.task(name="hello")
# def hello() -> None:
#     """Say which layer answered."""
#     print("{{ import_path }}")
