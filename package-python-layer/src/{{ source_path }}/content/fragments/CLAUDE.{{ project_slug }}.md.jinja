<!-- Shipped as {{ import_path }} layer content, delivered to the workspace
     by the sync verb. Edit this copy in the layer and release it;
     an edited delivered copy is a local override, kept and named.
-->
# The {{ project_slug }} layer's rules

The rules this layer adds to every workspace that mounts it. State
what the layer enforces; the base guidance already covers voice and
documentation.
