<!-- Seeded from the template channel (package-python-layer kind) at
     birth; this file is the workspace's own. Edit it directly:
     the template never rewrites it.
-->
# {{ package_name }}

{{ package_description }}
